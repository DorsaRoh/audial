# cadence environment configuration
# copy this file to .env.local and fill in your values

# no server-side environment variables are required for basic operation.
# api keys are entered in the browser settings modal and stored in localStorage.

# optional: node environment (development | production)
NODE_ENV=development
